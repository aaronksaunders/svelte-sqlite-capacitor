import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "svelte.sqlite.capacitor",
  appName: "svelte-sqlite-capacitor",
  webDir: "dist",
};

export default config;
